# Python deps for the perception and mission code (install into .venv, see scripts/setup.sh).
# ROS 2 packages (rclpy, cv_bridge, sensor_msgs, vision_msgs, mavros) come from apt, not pip.
numpy>=1.26,<2        # ROS 2 Humble cv_bridge is built against numpy 1
opencv-python-headless>=4.8,<5   # 5.x needs numpy 2
ultralytics==8.4.150
pymavlink>=2.4.41
pytest>=8
lark>=1.1      # keeps ROS 2 launch importable when pytest runs inside the venv
# torch and torchvision: install the build matching your driver, e.g.
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
